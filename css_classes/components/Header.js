import React from "react";

function Header() {
    return (
        <header className="something"> This is my header</header> // in JSX we use className which is related to the DOM,
    )                                                             // not class. eg: document.getblablabla.className etc
    }

export default Header;